#ifndef SISTEMA_DECLARACIO_FUNCIONS_H_
#define SISTEMA_DECLARACIO_FUNCIONS_H_

#include <stdlib.h>

#include "../1-Executor/descriptor.h"

int igual_general	(size_t, struct element_execucio*, struct funcio_dinamica*); // =
int extreu_punter	(size_t, struct element_execucio*, struct funcio_dinamica*); // & (var)

#endif // SISTEMA_DECLARACIO_FUNCIONS_H_
